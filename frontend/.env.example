# Backend API (Express on port 3000 locally)
# Mock login only if backend runs with npm run dev (NODE_ENV=development + ALLOW_DEV_AUTH_MOCK=true). Production uses real users only.
VITE_API_URL=http://localhost:3000
VITE_MAP_LAT=39.246
VITE_MAP_LNG=-82.478
VITE_MAP_ZOOM=13
